version: 2

build:
  os: ubuntu-lts-latest
  tools:
    python: "3"

sphinx:
  builder: html
  configuration: doc/conf.py
  fail_on_warning: true

formats: all

python:
  install:
    - method: pip
      path: .
      extra_requirements:
        - docs
