<euler1d-input>
<%doc>
var=val
</%doc>

  <!-- define attributes that can be modified by user -->

  <Attrib key="tstart" type="double" default="0.0" />
  <Attrib key="tend" type="double" />
  <Attrib key="numFrames" type="integer" default="1"/>
  <Attrib key="xlower" type="double" />
  <Attrib key="xupper" type="double" />
  <Attrib key="cells" type="integer" />

  <plots>
    <!-- Insert python plot commands here -->
  </plots>

  <template>
    <!-- Insert the input file template here -->
<![CDATA[
tStart = ${data["tstart"]}
tEnd = ${data["tend"]}
numFrames = ${data["numFrames"]}
initDt = 0.1
verbosity = debug

  <Grid domain>
    kind = cart1D
    lower = [${data["xlower"]}]
    upper = [${data["xupper"]}]
    cells = [${data["cells"]}]
  </Grid>
]]>
  </template>

</euler1d-input>

