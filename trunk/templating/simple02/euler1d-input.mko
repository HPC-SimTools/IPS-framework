##available to all input files
<%
tstart=0.0
tend=1.0
numFrames=10
xlower=-1.0
xupper=1.0
cells=16
%>

<%doc> Insert the input file template here </%doc>

<euler1d-input>

tStart = ${tstart}
tEnd = ${tend}
numFrames = ${numFrames}
initDt = 0.1
verbosity = debug

  <Grid domain>
    kind = cart1D
    lower = [${xlower}]
    upper = [${xupper}]
    cells = [${cells}]

    <% for i in ${nspecies}
      import dataFile${dataFiles[i]}
    %>
  </Grid>

</euler1d-input>
